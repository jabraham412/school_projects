/*
   Header file. Only contains the prototypes of my two functions: my_nextfit_malloc, and my_free;
   which are defined in mymalloc.c
*/

void *my_nextfit_malloc( int size );

void my_free( void *ptr );
